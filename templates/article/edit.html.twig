{% extends "base.html.twig" %}

{% block body %}

    <h1>Modifier un article</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-success">Enregistrer les modifs</button>
    {{ form_end(form) }}

{% endblock %}